<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="onNavigatingTo">
    <Page.actionBar>
        <ActionBar class="action-bar">
          <StackLayout>
             <Image src="~/images/tittel.png" class="iconTitle" ios.position="center"></Image>
          </StackLayout>
            <ActionBar.actionItems>
                <ActionItem text="Ny" tap="{{ insert }}" ios.position="right" />
            </ActionBar.actionItems>
        </ActionBar>
    </Page.actionBar>
        <ListView items="{{ lists }}" class="listLayout">
            <ListView.itemTemplate>
                <StackLayout>
                    <Label text="{{ list_name }}" class="list-view-item" tap="markDone"/>
                </StackLayout>
            </ListView.itemTemplate>
        </ListView>
</Page>
